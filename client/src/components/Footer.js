const Footer = () => {
  const style = {
    footer: {
      position: "fixed",
      bottom: 0,
      width: "100%",
    },
  };
  return <h1 style={style.footer}>Footer</h1>;
};

export default Footer;
